# @package _global_

# Minimal trainrun configuration for cuvis-ai-core testing
# Uses only basic mock nodes (no cuvis-ai dependencies)

defaults:
  - /data@data: lentils
  - /training@training: default
  - _self_

name: gradient_based

# Pipeline reference, resolved relative to this trainrun's directory.
pipeline: ../pipeline/gradient_based.yaml

# Optional fields for compatibility (empty since we don't have these nodes in basic mock setup)
unfreeze_nodes:
- SoftChannelSelector

freeze_nodes: []

loss_nodes:
- SimpleLossNode

metric_nodes:
- MockMetricNode

output_dir: outputs/gradient_based
